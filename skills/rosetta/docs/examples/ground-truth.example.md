# Ground Truth — example-app  *(illustrative example output)*

> This is a sanitized example of what Rosetta writes to `<project>/.agents/ground-truth.md`. The
> project, agents, and sessions are fictional; it shows the structure and the citation style.

_Generated by Rosetta · 2026-06-08T12:00:00Z · /Users/you/code/example-app · run `full`_

## Coverage report

5 agents scanned · 41 sessions · 612 messages · Feb–Jun 2026.

| Agent | Sessions | Messages | Match mode | Notes |
|---|---|---|---|---|
| Claude Code | 18 | 402 | path-encoded dir | — |
| Codex | 9 | 121 | scan + cwd filter | `sessions_without_cwd: 3` (old schema, unattributable) |
| Gemini CLI | 7 | 54 | tmp/<name>/chats | — |
| Aider | 5 | 31 | per-project markdown | — |
| Hermes | 2 | 4 | fuzzy (path mention) | lower confidence |
| Cursor | 0 | 0 | cursor-encoded dir | none for this project |

`unknown_stores`: none. Unmatchable history surfaced, not hidden.

## Current state (verified against code/git)

`example-app` is a TypeScript service on Postgres, deployed to Fly.io. `main` is green; the in-flight
work is a billing rewrite on branch `feat/usage-billing` (12 commits ahead, not merged).

## Architecture & key components

- HTTP layer `src/api/` (Fastify), domain `src/core/`, persistence `src/db/` (Drizzle).
- Background jobs via a Postgres-backed queue (`src/jobs/`), not Redis — see Decisions.
  `claude · 7f3a… · 2026-04-22`

## Decisions & rationale (cited)

- **Postgres-backed job queue, not Redis** — avoided a second datastore; throughput was sufficient at
  current scale. `claude · 7f3a… · 2026-04-22` (confirmed in `src/jobs/queue.ts`).
- **Drizzle over Prisma** — lighter migrations, SQL-first. `codex · 019e… · 2026-03-30`.
- **Usage-based billing** — *intended*, not shipped: discussed across 3 sessions; code exists only on
  `feat/usage-billing`. Recorded as Proposed, not done. `claude · a1b2… · 2026-06-01`

## Open questions / unresolved

- Whether to meter by request or by compute-second — unresolved across the billing sessions.

## Abandoned / reverted approaches

- A Redis queue was prototyped and reverted (`git revert 9c1d…`) — do not re-attempt without new
  throughput evidence. `claude · 7f3a… · 2026-04-22`

## TODOs / next steps

- Land `feat/usage-billing` behind a flag; write the metering-granularity ADR first.

## Contradictions & unverified claims

- A transcript claims "billing is live in staging" (`claude · a1b2… · 2026-06-01`) — **refuted**: no
  billing tables in any migration on `main`. Demoted to intended.

## Provenance index

| session-id | agent | date | role |
|---|---|---|---|
| `7f3a…` | Claude Code | 2026-04-22 | queue decision + Redis revert |
| `019e…` | Codex | 2026-03-30 | ORM choice |
| `a1b2…` | Claude Code | 2026-06-01 | billing design (intended) |
