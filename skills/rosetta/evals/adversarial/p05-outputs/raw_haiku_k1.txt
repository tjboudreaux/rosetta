The current primary datastore for the user-profile service is Postgres, adopted on 2025-11-20 per the engineering log. The profile_store.py code shows DynamoDB as legacy and the service is in mid-migration, while the most recent decision (2026-05-28) notes that "DynamoDB tables still present during migration," confirming the active migration from DynamoDB to Postgres for relational data needs.
