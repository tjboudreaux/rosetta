# ADR NNNN — <short title>

- Status: Proposed
- Date: YYYY-MM-DD
- Decided originally: YYYY-MM-DD
- Decider: <name>
- Sources: <provenance — `agent · session-id · date`, a git commit, a code path, or a task id>
- Related: <sibling ADR/PDR/BDR ids, code paths, docs>

## Context

What forces this decision? The problem, constraint, or prior decision in play. Keep it to the
forces that actually shaped the call — not a status report.

## Decision

What we will do, stated plainly.

## Consequences

Positive:
- What becomes easier / safer / cheaper.

Negative:
- What becomes harder, and how it is mitigated.

## Alternatives considered

- **Option A** — why not.
- **Option B** — why not.

## Related

- Code paths, docs, sibling decisions. If this reverses a prior decision, name the ID it supersedes
  (and set that record's Status to `Superseded by ADR NNNN`) — every reversal is recorded so the
  project never silently oscillates.
